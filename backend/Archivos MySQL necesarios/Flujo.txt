[Admin] → crea ciclo → agrega cursos y docentes → publica ofertas → define horarios
     ↓
[Alumno] → se registra → elige curso o paquete → se matricula (pendiente)
     ↓
[Admin] → revisa voucher → acepta matrícula → activa acceso
     ↓
[Docente] → marca asistencias
     ↓
[Eventos automáticos]
     ├─ avisa por 3 faltas
     ├─ avisa por cuotas vencidas
     └─ limpia notificaciones viejas
     ↓
[Admin Dashboard] ← ve reportes analíticos (asistencia + pagos)
